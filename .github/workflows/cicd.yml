name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      dotnet-logging:
        required: true
        type: choice
        default: minimal
        options:
          - quiet
          - minimal
          - normal
          - detailed
          - diagnostic

jobs:
  all:
    if: github.run_id != 1
    name: Build & Tests
    uses: dailydevops/pipelines/.github/workflows/build-dotnet-matrix.yml@0f1aaf8cf9fce642b4944f9874247ebd5413e39e # 2.3.150
    with:
      disableTestsOnLinux: false
      disableTestsOnMacOs: false
      disableTestsOnWindows: false
      dotnetLogging: ${{ inputs.dotnet-logging || 'minimal' }}
      dotnetVersion: ${{ vars.NE_DOTNET_TARGETFRAMEWORKS }}
      failFast: false
      solution: ./ArchiDuct.slnx
    secrets: inherit
